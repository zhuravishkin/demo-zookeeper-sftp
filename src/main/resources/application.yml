service:
  nodes-path: ${NODES_PATH:/app}
  lock-path: ${LOCK_PATH:/lock}
  session-timeout-ms: ${SESSION_TIMEOUT_MS:10000}
  connection-timeout-ms: ${CONNECTION_TIMEOUT_MS:9000}
  delayed-start-sec: ${DELAYED_START_SEC:10}
